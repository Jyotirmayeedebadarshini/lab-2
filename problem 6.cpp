#include <iostream>
#include <math.h>
using namespace std;
 
int main() {
	float a,s;
	cout<< "enter a number which squreroot is to be calculated:";
	cin>>a;
	s=pow(a,0.5);
	cout<<"squreroot of a is:"<<s;
	return 0;
}
